datasource db {
  provider = "postgres"
  url      = env("DATABASE_URL")
}

generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["postgresqlExtensions"]
}

model Vehicle {
  uuid             String   @id @default(uuid()) @unique
  rendszam         String   @unique
  tulajdonos       String
  forgalmi_ervenyes String
  adatok           String[]
  @@index([rendszam, tulajdonos, adatok])
}